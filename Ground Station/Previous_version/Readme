This folder contains:
--> First_generation:
---> This folder contains the very first set of codes we have generated. These codes are very basic GUIs and not necessarily focused on achieving the goal of transmitting or receiving data. 
--> Second_generation:
---> This folder contains the second set of codes we have generated. The codes inside this folder are Python scripts that meet certain criteria that include certain commands, graphs, and layout of the GUI. 
--> Ground_station_alpha_version :
---> This folder contains Python files that have undergone heavy changes between each iteration. These Python scripts are the transition edits between each generation, until the final version. 
(The higher the number it is somewhat closer to the final version.)
